# Supabase devcontainer feature

Install Supabase for use with Docker-in-Docker (via docker compose)

## Example Usage

```json
"features": {
    "ghcr.io/tgoz/devcontainer-features/supabase:0.0.1": {}
}
```
